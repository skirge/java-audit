CREATE TABLE loads (
    machine inet,
    mtime timeuuid,
    load float,
    PRIMARY KEY ((machine, cpu), mtime)
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTE󠁭RING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDเER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY (mtime DESC);
) WITH CLUSTERING ORDER BY ;
